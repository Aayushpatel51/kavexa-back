# PostgreSQL connection string (Prisma CLI + runtime via @prisma/adapter-pg)
# Replace user, password, host, port, and database name for your server.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/nest_lms?schema=public"
